<div class="container-xl">
    <div class="navbar-nav flex-row order-md-last">
        <div class="nav-item dropdown d-none d-md-flex me-3">
            <a type="button" class="nav-link px-0" href="{{ path('settings') }}" title="Einstellungen">
                {{ ux_icon('tabler:settings') }}
            </a>
        </div>
    </div>
    <div class="collapse navbar-collapse" id="navbar-menu">
        <div class="d-flex flex-column flex-md-row flex-fill align-items-stretch align-items-md-center">
            <ul class="navbar-nav">
                <li class="nav-item"><a href="{{ path('chat') }}" class="nav-link">{{ ux_icon('tabler:message') }}&nbsp;{{ settings_handler.get.chatbotName }}</a></li>
                <li class="nav-item"><a href="{{ path('documents_overview') }}" class="nav-link">{{ ux_icon('tabler:books') }}&nbsp;Bibliothek</a></li>
                {% block additional_menu %}{% endblock %}
            </ul>
        </div>
    </div>
</div>
